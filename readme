##to start
#login to your docker account 
#put the db_dump.sql file and .env (backend)file in the same root that docker-compose file exists.
#put .env files for backend and frontend
# then go to docker-compose dir 
# docker-compose up --build

#if you want to write some command in backend container: docker-compose exec backend sh 
#if you want to write commands in frontend container: docker-compose exec frontend sh 
